<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiapi.project.mapper.UserPointsMapper">

    <resultMap id="BaseResultMap" type="com.qiapi.qiapicommon.model.entity.UserPoints">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="userId" jdbcType="BIGINT"/>
        <result property="totalPoints" column="totalPoints" jdbcType="BIGINT"/>
        <result property="availablePoints" column="availablePoints" jdbcType="BIGINT"/>
        <result property="frozenPoints" column="frozenPoints" jdbcType="BIGINT"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="updateTime" jdbcType="TIMESTAMP"/>
        <result property="isDelete" column="isDelete" jdbcType="TINYINT"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,userId,totalPoints,availablePoints,frozenPoints,
        createTime,updateTime,isDelete
    </sql>

    <!-- 扣减用户积分 -->
    <update id="deductPoints">
        UPDATE user_points 
        SET availablePoints = availablePoints - #{amount},
            updateTime = NOW()
        WHERE userId = #{userId}
          AND availablePoints >= #{amount}
          AND isDelete = 0
    </update>

    <!-- 增加用户积分 -->
    <update id="addPoints">
        UPDATE user_points 
        SET totalPoints = totalPoints + #{amount},
            availablePoints = availablePoints + #{amount},
            updateTime = NOW()
        WHERE userId = #{userId}
          AND isDelete = 0
    </update>

</mapper>